package com.fraud.transaction.config;

import org.springframework.context.annotation.Configuration;

/**
 * Vault / Secrets Manager integration placeholder.
 * - Use Spring Cloud Vault or Spring Cloud AWS/Azure KeyVault clients.
 * - Keep secrets outside of application.yml in production.
 */
@Configuration
public class VaultConfig {
    // TODO: Add Vault configuration or bootstrap properties to fetch DB credentials and other secrets.
}
