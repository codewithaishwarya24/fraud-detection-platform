package com.fraud.transaction.config;

import org.springframework.context.annotation.Configuration;

/**
 * Web configuration: CORS, Interceptors, Formatters.
 * Keep cross-cutting web concerns here (but avoid business logic).
 */
@Configuration
public class WebConfig {
}
